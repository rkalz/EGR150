%"if" statements are used to perform an operation only if the statement is
%true. All if statements are coupled with a "end" to end the loop. "elseif"
%or "else" can be used in partner in bewtween the "if" and the "end". 

%%Example #1 if 

%Example Vector Creation
 %"if" this statement is true
    
 %"then" this line will run
  
 %if the statement is not true, then this line will run 
    
 %ends the if statement EVERY IF NEEDS AN END 

%% Example #2 if loop
%Use an if statement to assign letter grades to students in a class.
%Individuals should be able to provide any input and the loop will output
%the correct letter grade using the following scale:
%grade>=89.5 A
%79.5<=grade<89.5 B
%69.5<=grade<79.5 C
%59.5<=grade<69.5 D
%grade<59.5 F
%Include an error message if an invalid grade is input.
 
 %%Example #3 switch

%Example scalar creation
%grade (allow users to input grade)
%averageGrade

%switches the variable "grade"


       

    